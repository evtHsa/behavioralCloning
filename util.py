

import pdb

def parse_csv(csv_path):
    with open(csv_path) as f:
        pdb.set_trace()
        #lines = list(csv.reader(f, skipinitialspace=True, delimiter=',', quoting=csv.QUOTE_NONE))
        pass
